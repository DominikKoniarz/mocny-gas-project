This app is the Secure Software Update System.

It consists of:
/electron - electron desktop app - software update client
/web - software update server with a dashboard

When doing `pnpm install` do not create .pnpm_store

Always try to install outside the sandbox. If not possible ask the user to install for you.
